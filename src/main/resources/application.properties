spring.application.name=emailserver
# Dummy SMTP properties for demonstration.
# In a real scenario, these would be actual, secure credentials and SMTP configurations.
# For Gmail, you might need to enable "Less secure app access" or use App Passwords,
# which are deprecated. For a real system, OAuth2 or proper SMTP authentication is required.
# As these are dummy credentials, actual email sending will likely fail for real SMTP servers.

# Gmail
email.vendors.gmail.host=smtp.gmail.com
email.vendors.gmail.port=587
email.vendors.gmail.username=admin@gmail.com # Use a real Gmail address if testing actual send
email.vendors.gmail.password=admin           # Use a real password if testing actual send
email.vendors.gmail.postfix=@gmail.com

# Walla
email.vendors.walla.host=smtp.walla.co.il
email.vendors.walla.port=587
email.vendors.walla.username=admin@walla.co.il # Use a real Walla address if testing actual send
email.vendors.walla.password=admin             # Use a real password if testing actual send
email.vendors.walla.postfix=@walla.co.il

# Yahoo
email.vendors.yahoo.host=smtp.yahoo.com
email.vendors.yahoo.port=587
email.vendors.yahoo.username=admin@yahoo.com # Use a real Yahoo address if testing actual send
email.vendors.yahoo.password=admin           # Use a real password if testing actual send
email.vendors.yahoo.postfix=@yahoo.com

# JavaMail properties for all vendors
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=*
*/